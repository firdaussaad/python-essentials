#!/usr/bin/env python3.6

# strings are just plain text. Only need "" when storing a string.
# bullion value is a true or false value
# can only concatenate str (not "int") to str. Add str

character_name = "John"
character_age = 35
is_male = True
print("There was once a man name " + character_name + ",")
print("He was " + str(character_age) + "years old")

character_name = "Mike"
print("He really liked the name " + character_name + ", ")
print("but didn,t like being " + str(character_age))
